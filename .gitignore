# =============================================================================
# C# / .NET build outputs
# =============================================================================
**/bin/
**/obj/
*.user
*.suo
*.userprefs
.vs/
.vscode/
.idea/

# MSBuild user-local overrides — see Directory.Build.props for the contract.
# Each contributor sets their local BannerlordPath here without leaking it
# into the repo.
Directory.Build.props.user

# Solution-level user settings
*.sln.docstates
*.sln.iml

# JetBrains Rider
_ReSharper.Caches/
_TeamCity*/

# =============================================================================
# Decompiled vanilla & 3rd-party mod sources — kept locally as reference,
# but excluded from git (third-party copyright + repo bloat).
# =============================================================================
_research/
**/_research/

# =============================================================================
# Internal documentation — design specs, behaviour guides, audit reports,
# planning notes. Local-only by project policy (only README.md ships).
# =============================================================================
audits/
docs/

# =============================================================================
# AI tooling artefacts — local-only conventions / scratch state, not project code.
# Anything Claude Code / Anthropic Agent SDK / similar drops in the repo.
# =============================================================================
.claude/
.worktrees/
CLAUDE.md
AGENTS.md
GEMINI.md
RTK.md

# =============================================================================
# OS / editor cruft
# =============================================================================
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~
