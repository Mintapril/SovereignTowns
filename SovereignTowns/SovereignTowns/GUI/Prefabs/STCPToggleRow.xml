<Prefab>
  <Window>
    <!-- One feature-switch row: [toggle button] [label + hint text] -->
    <ListPanel WidthSizePolicy="StretchToParent" HeightSizePolicy="CoverChildren"
               StackLayout.LayoutMethod="HorizontalLeftToRight"
               VerticalAlignment="Center"
               MarginBottom="4">
      <Children>

        <!-- Toggle button (checkbox style): ButtonType=Toggle binds IsSelected to IsChecked -->
        <ButtonWidget DoNotPassEventsToChildren="true"
                      ButtonType="Toggle"
                      IsSelected="@IsChecked"
                      WidthSizePolicy="Fixed" HeightSizePolicy="Fixed"
                      SuggestedWidth="28" SuggestedHeight="28"
                      VerticalAlignment="Center"
                      MarginRight="10"
                      Sprite="BlankWhiteSquare_9"
                      Color="#B8960CFF"
                      AlphaFactor="0.3"
                      UpdateChildrenStates="true">
          <Children>
            <!-- Check indicator: visible when button IsSelected -->
            <Widget WidthSizePolicy="Fixed" HeightSizePolicy="Fixed"
                    SuggestedWidth="16" SuggestedHeight="16"
                    HorizontalAlignment="Center" VerticalAlignment="Center"
                    Sprite="BlankWhiteSquare_9"
                    Color="#B8960CFF"
                    AlphaFactor="0.0"
                    Id="CheckMark">
            </Widget>
          </Children>
        </ButtonWidget>

        <!-- Label + hint column -->
        <ListPanel WidthSizePolicy="StretchToParent" HeightSizePolicy="CoverChildren"
                   StackLayout.LayoutMethod="VerticalBottomToTop"
                   VerticalAlignment="Center">
          <Children>
            <TextWidget WidthSizePolicy="StretchToParent" HeightSizePolicy="CoverChildren"
                        Text="@Label"
                        VerticalAlignment="Center" />
            <TextWidget WidthSizePolicy="StretchToParent" HeightSizePolicy="CoverChildren"
                        Text="@Hint"
                        VerticalAlignment="Center"
                        MarginTop="2" />
          </Children>
        </ListPanel>

      </Children>
    </ListPanel>
  </Window>
</Prefab>
