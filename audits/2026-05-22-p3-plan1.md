# P3 Plan 1 实现计划 —— A(可配置 tick) + D(派发风险)

> **For agentic workers:** REQUIRED SUB-SKILL: 用 superpowers:subagent-driven-development 或
> superpowers:executing-plans 逐任务实现。步骤用 `- [ ]` 复选框。
>
> **本计划是 P3 一揽子重构的第 1 份**(共 2 份)。Plan 2 = B1+B2+C(时间展开 solver +
> 威胁预测器),待本计划落地 + 时间展开图编码经 advisor 锁定后再写。
> 设计依据:`audits/2026-05-22-p3-lookahead-design.md`。

**Goal:** 让首府后勤评估间隔可配置(默认 6h),并把「派发队伍的路途危险」作为成本 /
否决维度接入派发层与合并 solver —— 修复「征兵队被派进敌军送死」的 live bug。

**Architecture:** 新增只读 `HostilePartyScanner`(复用 vanilla
`MobileParty.StartFindingLocatablesAroundPosition` 定位 API)。派发层
(`ExecuteRecruiterDispatch`/`ExecuteTransferDispatch`,legacy+merged 共用)加 soft-skip
否决;合并 solver 招募/调拨边加 routeRisk 成本项。tick 入口从 `DailyTickEvent` 迁到
`HourlyTickEvent` + 无状态间隔门控。

**Tech Stack:** C# net472,Bannerlord v1.3.15 Campaign API,`MinCostFlow`(项目内 MCMF 引擎)。

**项目约定(重要):**
- **无单元测试** —— 验证 = `dotnet build` 0 errors / warnings 不增基线(57) + 读码 trace。
  改 live 行为的部分(Part A、D1)**本计划无法 in-game 验证,须用户实测**。
- **提交需用户明确要求** —— 每个 Task 末尾的 commit 步骤须等用户批准;未批准则停在该步。
- vanilla API 改动引用 `SovereignTowns/_research/` 反编译源(CLAUDE.md 规范)。

---

## 文件结构

| 文件 | 角色 | 动作 |
|---|---|---|
| `src/Configuration/FiscalAutonomyConfig.cs` | 配置 DTO | 改:加 4 字段 + 改名 1 字段 |
| `src/Evaluators/HostilePartyScanner.cs` | 敌对队伍空间扫描器 | **新建** |
| `src/Campaign/SovereignTownsCampaignBehavior.cs` | 事件分发器 | 改:tick 入口迁移 |
| `src/Algorithm/UnifiedGarrisonSolver.cs` | 合并 solver | 改:per-tick 遣散折算 + routeRisk 成本项 |
| `src/Managers/CapitalLogisticsManager.cs` | 后勤调度入口 | 改:派发层风险否决 |
| `audits/mcmf-merge-handoff.md` | 合并 solver 交接文档 | 改:§3.4/§5 同步改名 |

---

## Task 1 — Part A:配置字段(加间隔字段 + 遣散上限改名)

**Files:** Modify `src/Configuration/FiscalAutonomyConfig.cs`

- [ ] **Step 1: 加 tick 间隔字段。** 在 `// ── 金库 / 预算 ──` 段末尾(`PlayerClanSubsidyWhenTreasuryEmpty` 之后)加:

```csharp
    /// <summary>首府后勤评估间隔(小时)。默认 6h;读取时 clamp [1,24]。
    /// 改变「一个 tick = 多久」—— P3 时域 T、遣散速率均以此为单位。</summary>
    public int CapitalLogisticsTickHours { get; set; } = 6;
```

- [ ] **Step 2: 遣散上限改名 + 改「每天」口径。** 把字段

```csharp
    public int MergedDisbandPerTickCap { get; set; } = 20;
```

连同其 XML 注释整体替换为:

```csharp
    /// <summary>合并 solver 两段 bypass(M4):每城每【天】经「正常段」遣散的头数上限。
    /// solver 按 <see cref="CapitalLogisticsTickHours"/> 折算成每 tick 上限。
    /// 设 0 = 关正常遣散段,只保留溢出段。详见 audits/mcmf-merge-handoff.md §3.4。</summary>
    public int MergedDisbandPerDayCap { get; set; } = 20;
```

- [ ] **Step 3:** 同文件内 `MergedBypassOverflowPenalty` 的 XML 注释引用了
  `MergedDisbandPerTickCap`,改成 `MergedDisbandPerDayCap`。

- [ ] **Step 4: 构建验证。**

Run: `dotnet build SovereignTowns/src/SovereignTowns.csproj -c Debug`
Expected: **报错** —— `UnifiedGarrisonSolver.cs` 仍引用旧名 `MergedDisbandPerTickCap`。
这是预期的(Task 3 修复);本步只确认改名已生效、无拼写错。

---

## Task 2 — Part A:tick 入口迁移 + solver per-tick 折算

**Files:**
- Modify `src/Campaign/SovereignTownsCampaignBehavior.cs`
- Modify `src/Algorithm/UnifiedGarrisonSolver.cs:~330`

- [ ] **Step 1: 订阅 HourlyTickEvent。** 在 `RegisterEvents` 里 `DailyTickEvent` 那行附近加:

```csharp
            CampaignEvents.HourlyTickEvent.AddNonSerializedListener(this, OnHourlyTick);
```

- [ ] **Step 2: OnDailyTick 移除 EvaluateAll。** 把 `OnDailyTick` 内这一行删除:

```csharp
            _capitalLogisticsManager?.EvaluateAll();
```

(`DrainWebConfigSync()`、每日活动汇总等其余逻辑保留不动。)

- [ ] **Step 3: 新增 OnHourlyTick 方法。** 在 `OnDailyTick` 方法之后加:

```csharp
    /// <summary>
    /// 首府后勤评估入口。从 vanilla HourlyTickEvent(Action,无参)进入,
    /// 用无状态间隔门控:CampaignTime 总小时数能被 CapitalLogisticsTickHours 整除时
    /// 才评估。无状态 → 存档/读档相位自动对齐,无需持久化计数器。
    /// </summary>
    private void OnHourlyTick()
    {
        try
        {
            int interval = ConfigurationManager.Current?.FiscalAutonomy?.CapitalLogisticsTickHours ?? 6;
            if (interval < 1) interval = 1;
            if (interval > 24) interval = 24;
            if ((long)CampaignTime.Now.ToHours % interval != 0) return;
            DrainWebConfigSync();
            _capitalLogisticsManager?.EvaluateAll();
        }
        catch (Exception ex)
        {
            Logger.Error("OnHourlyTick failed", ex);
        }
    }
```

> 注:`ConfigurationManager` / `CampaignTime` / `Logger` 在本文件其它方法已使用同名引用 —
> 若编译报「未找到」,按文件顶部既有 using / 别名补齐。

- [ ] **Step 4: solver per-tick 遣散折算。** `UnifiedGarrisonSolver.cs` 中

```csharp
            int disbandPerTickCap = Math.Max(0, cfg.MergedDisbandPerTickCap);
```

替换为:

```csharp
            // MergedDisbandPerDayCap 是「每天」口径;按 tick 间隔折算成每 tick 上限。
            int tickHours = Math.Min(24, Math.Max(1, cfg.CapitalLogisticsTickHours));
            int disbandPerTickCap = Math.Max(0, (int)Math.Round(cfg.MergedDisbandPerDayCap * tickHours / 24.0));
```

- [ ] **Step 5: 构建验证。**

Run: `dotnet build SovereignTowns/src/SovereignTowns.csproj -c Debug`
Expected: **0 errors, 57 warnings**(基线,不得新增)。

- [ ] **Step 6: 提交(须用户批准)。**

```bash
git add SovereignTowns/src/Configuration/FiscalAutonomyConfig.cs SovereignTowns/src/Campaign/SovereignTownsCampaignBehavior.cs SovereignTowns/src/Algorithm/UnifiedGarrisonSolver.cs
git commit -m "feat(logistics): configurable evaluation interval (default 6h)"
```

---

## Task 3 — D-infra:`HostilePartyScanner`

**Files:** Create `src/Evaluators/HostilePartyScanner.cs`

> 仅实现 `HostileStrengthNear`(D1/D2 所需)。`EnumerateConvergingHostiles`(威胁预测用)
> 留到 Plan 2 的 Part C —— 现在写它就是未用代码(YAGNI)。

- [ ] **Step 1: 新建文件。** 完整内容:

```csharp
using System;
using TaleWorlds.CampaignSystem;
using TaleWorlds.CampaignSystem.Party;
using TaleWorlds.Library;
using Logger = SovereignTowns.Logging.Logger;

namespace SovereignTowns.Evaluators;

/// <summary>
/// 只读、无副作用的敌对队伍空间扫描器。基于 vanilla
/// MobileParty.StartFindingLocatablesAroundPosition 定位 API
/// (现有用例见 SallyForth/SallyDispatcher.FindBestEnemyTarget)。
/// 派发风险(Part D)与威胁预测(Part C,Plan 2)共用。
/// </summary>
public static class HostilePartyScanner
{
    /// <summary>
    /// <paramref name="point"/> 周围 <paramref name="radius"/> 内、对
    /// <paramref name="friendlyFaction"/> 敌对的军事队伍「健康兵力」之和
    /// (TotalManCount − TotalWounded)。0 = 安全。无副作用,可跨线程。
    /// </summary>
    public static float HostileStrengthNear(Vec2 point, float radius, IFaction friendlyFaction)
    {
        if (friendlyFaction == null || radius <= 0f) return 0f;
        float total = 0f;
        try
        {
            var search = MobileParty.StartFindingLocatablesAroundPosition(point, radius);
            for (var p = MobileParty.FindNextLocatable(ref search);
                 p != null;
                 p = MobileParty.FindNextLocatable(ref search))
            {
                if (!IsHostileMilitary(p, friendlyFaction)) continue;
                total += HealthyStrength(p);
            }
        }
        catch (Exception ex)
        {
            Logger.Error("HostilePartyScanner.HostileStrengthNear failed", ex);
        }
        return total;
    }

    /// <summary>该队伍是否为对 <paramref name="friendly"/> 敌对的、非玩家主队的活动队伍。</summary>
    private static bool IsHostileMilitary(MobileParty p, IFaction friendly)
    {
        if (p == null || !p.IsActive) return false;
        if (p == MobileParty.MainParty) return false;
        var f = p.MapFaction;
        if (f == null || f == friendly) return false;
        return f.IsAtWarWith(friendly);
    }

    /// <summary>队伍健康兵力 = TotalManCount − TotalWounded(镜像 SallyDispatcher 口径)。</summary>
    private static float HealthyStrength(MobileParty p)
    {
        try
        {
            var roster = p.MemberRoster;
            if (roster == null) return 0f;
            return Math.Max(0, roster.TotalManCount - roster.TotalWounded);
        }
        catch { return 0f; }
    }
}
```

- [ ] **Step 2: 构建验证。**

Run: `dotnet build SovereignTowns/src/SovereignTowns.csproj -c Debug`
Expected: **0 errors, 57 warnings**。若 `Vec2` / `IFaction` 报未找到,核对
`SallyDispatcher.cs` 的 using(同一套 API 在那里已编译通过)。

- [ ] **Step 3: 提交(须用户批准)。**

```bash
git add SovereignTowns/src/Evaluators/HostilePartyScanner.cs
git commit -m "feat(evaluators): add HostilePartyScanner for dispatch-risk scanning"
```

---

## Task 4 — D1:派发层风险否决(修 live bug)

**Files:**
- Modify `src/Configuration/FiscalAutonomyConfig.cs`
- Modify `src/Managers/CapitalLogisticsManager.cs`

- [ ] **Step 1: 加派发风险配置。** `FiscalAutonomyConfig.cs` 末尾(最后一个字段后、类闭合 `}` 前)加:

```csharp

    // ── 派发风险(Part D)──
    /// <summary>派发风险否决总开关(D1)。true = 路途有敌军时本 tick 不派征兵/调拨队。
    /// 直接影响 live 行为,可一键回退。</summary>
    public bool DispatchRiskEnabled { get; set; } = true;

    /// <summary>HostilePartyScanner 扫描半径(地图单位)。初值 30,须 in-game 调。</summary>
    public float DispatchRiskScanRadius { get; set; } = 30f;

    /// <summary>D1 否决阈值:路线风险分(沿途最大敌对健康兵力)≥ 此值 → 本 tick 不派。
    /// 初值 60,须 in-game 调。</summary>
    public float DispatchRiskVetoThreshold { get; set; } = 60f;

    /// <summary>D2 路线风险→成本 的标度乘子:成本加项 = 风险分 × 此值。
    /// 须与 routing(数百~千)可比。初值 10,须 in-game 调。</summary>
    public int DispatchRiskCostScale { get; set; } = 10;
```

- [ ] **Step 2: 在 `CapitalLogisticsManager` 加路线风险 helper。** 在
  `ExecuteRecruiterDispatch` 方法之前加:

```csharp
    /// <summary>
    /// 路线风险分 = home 与各 waypoint 的端点 + 连线中点处、由
    /// <see cref="HostilePartyScanner"/> 评出的最大敌对健康兵力。0 = 安全。
    /// Part D1(派发否决)与 D2(solver 成本项)共用同一口径。
    /// </summary>
    private static float RouteRiskScore(Settlement home, IEnumerable<Settlement> waypoints)
    {
        try
        {
            var cfg = ConfigurationManager.Current?.FiscalAutonomy ?? new FiscalAutonomyConfig();
            var friendly = home?.MapFaction;
            if (friendly == null) return 0f;
            float radius = Math.Max(1f, cfg.DispatchRiskScanRadius);
            float worst = HostilePartyScanner.HostileStrengthNear(home.GetPosition2D, radius, friendly);
            foreach (var wp in waypoints)
            {
                if (wp == null) continue;
                worst = Math.Max(worst,
                    HostilePartyScanner.HostileStrengthNear(wp.GetPosition2D, radius, friendly));
                var mid = (home.GetPosition2D + wp.GetPosition2D) * 0.5f;
                worst = Math.Max(worst,
                    HostilePartyScanner.HostileStrengthNear(mid, radius, friendly));
            }
            return worst;
        }
        catch (Exception ex)
        {
            Logger.Error($"CapitalLogisticsManager.RouteRiskScore failed (home='{home?.StringId}')", ex);
            return 0f;   // 评估失败 → 不阻派发(保守:宁可派也不无故卡死)
        }
    }
```

- [ ] **Step 3: 征兵队派发前否决。** `ExecuteRecruiterDispatch` 方法体内,
  `if (itinerary == null || itinerary.Count == 0) return false;` 这行之后加:

```csharp
        var riskCfg = ConfigurationManager.Current?.FiscalAutonomy ?? new FiscalAutonomyConfig();
        if (riskCfg.DispatchRiskEnabled)
        {
            float risk = RouteRiskScore(returnSettlement, itinerary);
            if (risk >= riskCfg.DispatchRiskVetoThreshold)
            {
                Logger.Info(
                    $"DISPATCH-RISK recruiter skipped: route risk {risk:F0} ≥ threshold " +
                    $"{riskCfg.DispatchRiskVetoThreshold:F0} home='{returnSettlement?.StringId}' stops={itinerary.Count}");
                return false;
            }
        }
```

- [ ] **Step 4: 调拨队派发前否决。** `ExecuteTransferDispatch` 方法体内,
  方法第一行(`string reason = ...` 之前)加:

```csharp
        var riskCfg = ConfigurationManager.Current?.FiscalAutonomy ?? new FiscalAutonomyConfig();
        if (riskCfg.DispatchRiskEnabled)
        {
            float risk = RouteRiskScore(instruction.Source, new[] { instruction.Destination });
            if (risk >= riskCfg.DispatchRiskVetoThreshold)
            {
                Logger.Info(
                    $"DISPATCH-RISK transfer skipped: route risk {risk:F0} ≥ threshold " +
                    $"{riskCfg.DispatchRiskVetoThreshold:F0} src='{instruction.Source?.StringId}' " +
                    $"dst='{instruction.Destination?.StringId}'");
                return false;
            }
        }
```

> 注:出击队不经 `ExecuteRecruiterDispatch`/`ExecuteTransferDispatch`,天然不受影响
> (设计 §2)。`CapitalLogisticsManager` 顶部 using 已含 `SovereignTowns.Evaluators`
> (`RiskAssessmentService` 已用)→ `HostilePartyScanner` 可直接引用。

- [ ] **Step 5: 构建验证。**

Run: `dotnet build SovereignTowns/src/SovereignTowns.csproj -c Debug`
Expected: **0 errors, 57 warnings**。

- [ ] **Step 6: 提交(须用户批准)。**

```bash
git add SovereignTowns/src/Configuration/FiscalAutonomyConfig.cs SovereignTowns/src/Managers/CapitalLogisticsManager.cs
git commit -m "feat(logistics): dispatch-layer route-risk veto for recruiter/transfer parties"
```

---

## Task 5 — D2:合并 solver 的派发风险成本项

**Files:** Modify `src/Algorithm/UnifiedGarrisonSolver.cs`

- [ ] **Step 1: 加 routeRisk 成本 helper。** 在 `RoutingDistance` 方法之后加:

```csharp
    /// <summary>
    /// D2:路线风险加进 routing 成本。a↔b 端点 + 连线中点处的最大敌对健康兵力
    /// × DispatchRiskCostScale。`DispatchRiskEnabled` 关时返回 0。
    /// 软成本 —— 危险路线成本升高,solver 自行改选安全村 / 原地招募 / 不招。
    /// </summary>
    private static int RouteRiskSurcharge(Settlement a, Settlement b, FiscalAutonomyConfig cfg)
    {
        try
        {
            if (!cfg.DispatchRiskEnabled) return 0;
            var friendly = a?.MapFaction ?? b?.MapFaction;
            if (friendly == null || a == null || b == null) return 0;
            float radius = Math.Max(1f, cfg.DispatchRiskScanRadius);
            float worst = HostilePartyScanner.HostileStrengthNear(a.GetPosition2D, radius, friendly);
            worst = Math.Max(worst,
                HostilePartyScanner.HostileStrengthNear(b.GetPosition2D, radius, friendly));
            var mid = (a.GetPosition2D + b.GetPosition2D) * 0.5f;
            worst = Math.Max(worst,
                HostilePartyScanner.HostileStrengthNear(mid, radius, friendly));
            return (int)Math.Round(worst * Math.Max(0, cfg.DispatchRiskCostScale));
        }
        catch (Exception ex)
        {
            Logger.Error("UnifiedGarrisonSolver.RouteRiskSurcharge failed", ex);
            return 0;
        }
    }
```

- [ ] **Step 2: 招募边加成本项。** `Solve` 内招募 origin 出边处,村庄 routing 计算:

```csharp
                    int routing = o.Kind == OriginKind.Village
                        ? RoutingDistance(o.Settlement, capitalSettlement)
                          + Math.Max(0, thresholds.McmfRecruiterOverhead)
                        : 0;   // InPlace 原地招募,routing 0
```

替换为:

```csharp
                    int routing = o.Kind == OriginKind.Village
                        ? RoutingDistance(o.Settlement, capitalSettlement)
                          + Math.Max(0, thresholds.McmfRecruiterOverhead)
                          + RouteRiskSurcharge(o.Settlement, capitalSettlement, cfg)
                        : 0;   // InPlace 原地招募,routing 0、零暴露
```

- [ ] **Step 3: 现有驻军跨城调拨边加成本项。** `Solve` 内现有驻军 origin → 他城
  demand-tier 处:

```csharp
                            int routing = RoutingDistance(o.Settlement, dt.Settlement)
                                          + Math.Max(0, thresholds.McmfTransferOverhead);
```

替换为:

```csharp
                            int routing = RoutingDistance(o.Settlement, dt.Settlement)
                                          + Math.Max(0, thresholds.McmfTransferOverhead)
                                          + RouteRiskSurcharge(o.Settlement, dt.Settlement, cfg);
```

- [ ] **Step 4: transit→分支转发边加成本项。** `Solve` 内 transit 出边处:

```csharp
                        int routing = RoutingDistance(capitalSettlement, dt.Settlement)
                                      + Math.Max(0, thresholds.McmfTransferOverhead);
```

替换为:

```csharp
                        int routing = RoutingDistance(capitalSettlement, dt.Settlement)
                                      + Math.Max(0, thresholds.McmfTransferOverhead)
                                      + RouteRiskSurcharge(capitalSettlement, dt.Settlement, cfg);
```

> 三处均为加正值到既有 `routing`;`routing` 再与 `K − value` 相加 → 仍恒非负,
> 不触发 `MinCostFlow` 拒负费用。

- [ ] **Step 5: 构建验证。**

Run: `dotnet build SovereignTowns/src/SovereignTowns.csproj -c Debug`
Expected: **0 errors, 57 warnings**。

- [ ] **Step 6: 提交(须用户批准)。**

```bash
git add SovereignTowns/src/Algorithm/UnifiedGarrisonSolver.cs
git commit -m "feat(solver): route-risk cost term on recruiter/transfer edges"
```

---

## Task 6 — 同步 handoff 文档

**Files:** Modify `audits/mcmf-merge-handoff.md`

- [ ] **Step 1:** §3.4 与 §5 中所有 `MergedDisbandPerTickCap` 改为 `MergedDisbandPerDayCap`,
  并补一句:口径为「每天」,solver 按 `CapitalLogisticsTickHours` 折算成每 tick 上限。

- [ ] **Step 2:** §5 配置表加 `CapitalLogisticsTickHours` / `DispatchRisk*` 四个新字段行
  (默认值见 Task 1/4),并注明它们已由 `audits/2026-05-22-p3-lookahead-design.md` 引入。

- [ ] **Step 3: 提交(须用户批准)。**

```bash
git add SovereignTowns/audits/mcmf-merge-handoff.md
git commit -m "docs(handoff): sync disband-cap rename + dispatch-risk config"
```

---

## 验证总结

- **可由本计划验证**:全部 6 个 Task 的 `dotnet build` 0 errors / 57 warnings 不增。
- **须用户 in-game 实测**(改 live 行为,本计划无法验证):
  - Part A:6h tick 评估节奏正常,征兵/调拨/遣散仍按预期触发。
  - D1:敌军在首府/路线附近时,日志出现 `DISPATCH-RISK ... skipped`,征兵队不再出门送死;
    敌军离开后恢复派发。
  - D2:仅影响合并 solver(影子态),看 `MERGED-SHADOW`/`MERGED-DIFF` 日志即可,无 live 影响。
- **回退**:`DispatchRiskEnabled=false` 关 D1/D2;`CapitalLogisticsTickHours=24` 回每日 tick。

## 自审记录

- **spec 覆盖**:设计 spec 的 Part A(§4)→ Task 1/2;D-infra(§5.1)→ Task 3;
  D1(§5.2)→ Task 4;D2(§5.3)→ Task 5;§4.3 handoff 同步 → Task 6。B/C 属 Plan 2,
  本计划范围外(已在抬头声明)。
- **占位符**:阈值类配置(`DispatchRisk*`)给了具体默认值 + 「须 in-game 调」标注 ——
  非占位符,是项目既定的 in-game 调参流程(设计 spec §8 已述)。
- **类型一致**:`RouteRiskScore`(D1,float)与 `RouteRiskSurcharge`(D2,int)是两个不同
  helper,分别服务否决(比阈值)与成本(进 routing int);命名有意区分,非笔误。
  `HostilePartyScanner.HostileStrengthNear(Vec2, float, IFaction)` 签名在 Task 3/4/5 一致。
