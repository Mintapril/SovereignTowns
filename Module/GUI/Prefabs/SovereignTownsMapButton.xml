<Prefab>
  <Window>
    <!--
      大地图左侧、垂直居中的常驻入口按钮 —— 点击 → ExecuteOpen 打开控制面板。
      美术资源取自 vanilla 城镇菜单（GameMenu）左侧栏右上角的「折叠把手」按钮：
        Brush=GameMenu.Extend.Button       —— sprite SPGeneral\GameMenu\gamemenu_collapser_button,
                                              自带 Default/Hovered/Pressed/Selected 状态。
        Brush=GameMenu.Extend.Button.Arrow —— 把手内的箭头 sprite StdAssets\arrow_pointing_left。
      做成贴左屏边的竖向 tab 把手。
      约束：
        1. gamemenu_collapser_button 非 9-slice（无 _9 后缀、无 Extend）→ 不可拉伸,
           固定 58×122（与 GameMenu.xml 的 ExtendButton 同尺寸）,否则美术变形。
        2. 该 sprite 原设计「左侧贴面板、把手向右凸」—— 挂到屏幕左边沿同朝向,正常。
           箭头朝左是 vanilla 原图（原版由 GameMenuWidget 代码翻转,静态用无法翻转）。
        3. ButtonWidget 用 Brush 是设计内用法（非裸 Sprite,不会渲染成纯白）。
        4. UpdateChildrenStates=true → 把手 hover/pressed 状态传递给箭头子件,与原版一致。
      原先是 227×44 ButtonBrush2 文字按钮,本次按用户要求换成该折叠把手美术。
    -->
    <Widget WidthSizePolicy="CoverChildren" HeightSizePolicy="CoverChildren"
            HorizontalAlignment="Left" VerticalAlignment="Center">
      <Children>
        <ButtonWidget DoNotPassEventsToChildren="true" UpdateChildrenStates="true"
                      Command.Click="ExecuteOpen"
                      WidthSizePolicy="Fixed" HeightSizePolicy="Fixed"
                      SuggestedWidth="58" SuggestedHeight="122" Brush="GameMenu.Extend.Button">
          <Children>
            <BrushWidget WidthSizePolicy="Fixed" HeightSizePolicy="Fixed"
                         SuggestedWidth="30" SuggestedHeight="30"
                         VerticalAlignment="Center" MarginBottom="15" MarginLeft="8"
                         Brush="GameMenu.Extend.Button.Arrow" />
          </Children>
        </ButtonWidget>
      </Children>
    </Widget>
  </Window>
</Prefab>
